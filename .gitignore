# .gitignore for SPEKTRE GENESIS v1.1
# "Only keep what strengthens the Architecture."

# --- BLOCKING THE NOISE ---
/anxiety
/depressive_episodes
/limitations
/external_validation_requests
/bureaucracy.pdf
/hospital_menu.txt

# --- SYSTEM TRASH ---
*.log (Old narratives)
*.tmp (Fleeting doubts)
.DS_Store (Because macs are annoying, and so is systemic friction)
node_modules/ (We don't need bloat, we have pure logic)
dist/
.tools/
node_modules/
* 2.*
* 2
.DS_Store

# --- SECURITY ---
# Never commit your private keys, but always commit your public intent.
private_keys/
secrets.env (The Source connection string is internal only)

# --- ALLOWLIST (FORCE INCLUDE) ---
!src/core/
!SPEKTRE_GENESIS.md
!tuesday_plan.sh

# "Hups, I git-ignored my past and pushed only my future." :DDDD
# local caches / logs
.npm-cache/
.npm-logs/
*.log
genesis/*.log

# build artifacts
.next/
node_modules/
**/.next/
**/node_modules/
**/dist/
**/*.void